struct foo {
	int num
}

struct foo1 {
	int num
	string name	
}

struct ServerInfo {
	int id
	string name
	int status
}

proxy AccountServer {
	Login(string name, string password)
	Register(string name, string password)
	GetServers()
	EnterServer(int id)
}

proxy Player {
	LoginResult(int result) 
	RegisterResult(int result)
	UpdateServers(ServerInfo[] servers)	
	EnterServer(string name)
}



